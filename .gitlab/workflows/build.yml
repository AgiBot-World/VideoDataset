build:
  tags:
    - infini-gpu
  interruptible: true
  rules:
    - if: $CI_COMMIT_TAG =~ /^v?(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-?(a|b|rc)(0|[1-9][0-9]*)?)?$/
  artifacts:
    paths:
      - dist
  script:
    - make build
  stage: build
