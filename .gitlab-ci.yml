stages:
  - build
  - ci
  - release
default:
  before_script:
    - env | sort
  retry:
    max: 2
    when: runner_system_failure
  image: ${CI_REGISTRY_IMAGE}/agibot-forge/dev:25.04-latest

include:
  - local: .gitlab/workflows/**.yml
